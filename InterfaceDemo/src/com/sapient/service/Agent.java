package com.sapient.service;

public interface Agent {

	//int sum = 10;   //global constant which is public ... because in interface we cannot define the state
	void clearPdc();
	void approveLoan();
}
